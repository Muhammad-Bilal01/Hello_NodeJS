function add(a, b) {
  return a + b;
}

function sub(a, b) {
  return a - b;
}

// exports.add = (a, b) => a + b;

// ES MODULE
export function add(a, b) {
  return a + b;
}

// single export method
// module.exports = { add, sub };
